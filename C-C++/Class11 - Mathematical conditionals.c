#include <stdio.h>
#include <stdlib.h>

void main() {
    int x = 5, y = 10, z = 15;

    if (x > 2) {
        printf("\nThe variable x is bigger then 2");
    }

    if (z >= y) {
        printf("\nThe variable z is greater than or equal to the variable y");
    }

    if (x < 10) {
        printf("\nThe variable x is smaller than 10");
    }

    if (z != 10) {
        printf("\nThe variable z is diferrent of 10");
    }

}
