#include <stdio.h>
#include <stdlib.h>

void main() {
    char letter = 'n';

    if (letter == 'n') {
        printf("\nThe letter is %c", letter);
    }

    printf("\nLetter's code is %d", letter);

    if (letter == 110) {
        printf("\nThe code's letter is %c", letter);
    }

}
